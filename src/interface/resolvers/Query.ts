import { Context } from "./typings"

export default {
  ingredient(parent, params, {service}: Context) {
    service.
  },
  recipe(parent, params, {service}) {

  },
  technique(parent, params, {service}) {

  },
}
